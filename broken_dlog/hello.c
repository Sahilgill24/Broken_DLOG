#include <stdio.h>

int main(void) {
    printf("Hello, World!\n");
    printf("This is a C17 program.\n");
    return 0;
}
 // CUDA trial implementation for the work 
 // although 24gb is more than enough, the CPU ram is not 
 // the PCIe bus is the bottleneck, not the GPU memory
